import React from "react";
import { Text, View, StyleSheet, ScrollView } from "react-native";
import { theme } from "../../../../../../styling/index";

export const RecipePreview = () => {
    return (
        <ScrollView>
            <Text>PreScrollView</Text>
        </ScrollView>
    );
};

const styles = StyleSheet.create({
    container: {},
});
